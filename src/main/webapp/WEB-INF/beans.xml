<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                           https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0"
       bean-discovery-mode="all">

    <!--
      Enabling JmsNotificationService as an alternative makes it REPLACE
      LoggingNotificationService everywhere NotificationPort is injected.
      Comment this block out to fall back to plain logging (e.g. if the JMS
      resources are not created on the server).
    -->
    <alternatives>
        <class>com.mazadhub.notification.JmsNotificationService</class>
    </alternatives>
</beans>
